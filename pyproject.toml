[project]
name = "ml-senior-assignment"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "torch",
    "datasets",
    "tokenizers",
    "numpy",
    "scipy",
    "matplotlib",
    "pandas",
    "psutil>=7.2.2",
]
