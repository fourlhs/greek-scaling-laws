[project]
name = "greek-scaling-laws"
version = "0.1.0"
description = "Empirically derived scaling laws for a small Greek-language LM"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.11"
dependencies = [
    "torch",
    "datasets",
    "tokenizers",
    "numpy",
    "scipy",
    "matplotlib",
    "pandas",
    "psutil>=7.2.2",
    "weasyprint>=68.1",
    "jupyter>=1.1.1",
]
